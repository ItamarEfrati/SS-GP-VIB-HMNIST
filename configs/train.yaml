defaults:
  - _self_
  - datamodule: har/ssl_har
  - model: har/ss_gpvib_e_inception_d_flatten
  - trainer: gpu
  - logger: wandb
  - callbacks: default
  - hydra: default
  - debug:
  - paths: default
  - hparams_search:
  - metrics: tsc
  - extras: default

seed: 42
mode: debug
run_test: True
optimized_metric: ${metrics.optimize}

# evaluate on test set, using best model weights achieved during training
# lightning chooses best weights based on metric specified in checkpoint callback
ckpt_path: null # path to checkpoint model

# name of the run, should be used along with experiment mode
datamodule_name: har
model_name: ${model.name}
size: ssl_0.5
num_labeled: 0.5
suffix: _debug

